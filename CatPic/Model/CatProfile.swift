import Foundation
import SwiftUI

struct CatProfile: View {
    var cat: Cat

    var body: some View {
        ScrollView {
            VStack {
                AsyncImage(url: URL())
            }
        }
    }
}